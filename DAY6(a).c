#include <stdio.h>
int main() {
    int main()
    printf("Enter an integer: ");
    scanf("%d", &num);
    if (num % 2 == 0) {
        printf("%d is Even number.\n", num);
    } else {
        printf("%d isOdd number.\n", num);
    }
    return 0;
}
